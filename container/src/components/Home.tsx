import { SimpleButton, SparkButton } from "Shared/Components";

export const Home = () => (
  <div>
    <h1>Home Page</h1>
    <h4>
      Starting point of the Micro-FE's this a route at the container level
    </h4>
  </div>
);
